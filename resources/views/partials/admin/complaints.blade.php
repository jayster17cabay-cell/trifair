{{-- Shared complaints page body. Requires: $routePrefix (e.g. 'superadmin' | 'tfrb-officer'), $complaints, $filter, $pendingCount, $reviewedCount, $totalCount --}}

@include('partials.admin.complaint-list')
